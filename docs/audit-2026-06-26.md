# Auditoría UX — REP-10..20 (2026-06-26)

Auditoría del trabajo P0, P1 y follow-ups mergeados a `master`. Metodología: subagentes de
revisión de código + smoke manual en navegador (desktop + mobile 390px) + escaneo de consola.
Playwright no está en el repo; el smoke fue manual.

> **Actualización 2026-06-27:** PR #3 (P1) y PR #4 (REP-16/18/20) mergeados a `master`
> (`af73a13`). Este documento refleja el estado final post-merge.

## Estado del repo

- **`master`:** PR #1–#4 mergeados. `pnpm test` **62/62**, `typecheck` OK, `build` OK.
- **PRs cerrados:** #1 `AGENTS.md`, #2 P0 (REP-10/12/13), #3 P1 (REP-11/14/15/17 + 18/19),
  #4 follow-ups (REP-16/18/20).
- **Alcance:** sin tocar `office-scene` (visuales/animación intactos).

## Tabla de auditoría

| Issue | Prioridad | Estado | Evidencia | Gap / follow-up |
|---|---|---|---|---|
| REP-10 Unificar ciclo (12 meses) | P0 (Urgent) | ✅ | `lib/game-balance.ts` `CYCLE_TURNS=12`, guard `turn > MAX_TURNS`; `strategic-header.tsx` `Mes {turn}/{CYCLE_TURNS}`; `cycle-months.ts` 12 meses; `game-content.ts` "12 meses". | Ninguno. |
| REP-12 CTA "Siguiente mes" | P0 (Urgent) | ✅ | `strategic-header.tsx` hint en desktop; `mobile-decision-dock.tsx` guía + CTA en mobile. Tras PR #4 el dock recibe `isGameOver` y muestra "Ver resultado" con paridad al header. | Ninguno. |
| REP-13 "Jugar otro escenario" | P0 (Urgent) | ✅ | `app/page.tsx` `handlePlayAgain` → `setCurrentScreen("scenario")` + reset de estado. | Ninguno. |
| REP-11 Glosario unificado KPIs | P1 (High) | ✅ | `lib/kpi-glossary.ts` única fuente; consumido por strip, preview, teasers, modal. PR #4: `game-engine.ts` y `game-content.ts` usan `KPI_SHORT`. Oficina sin cambios (narrativa). | Ninguno. |
| REP-15 Pill abre detalle | P1 (High) | ✅ | `kpi-strip.tsx` pills `<button onClick>` + badge "Detalle". | Ninguno. |
| REP-17 "Detalle" visible en mobile | P1 (High) | ✅ | Badge visible en mobile 390px (sin `hidden sm:flex`). | Ninguno. |
| REP-14 Confirmar "Nuevo mandato" | P1 (High) | ✅ | `welcome-screen.tsx` + `alert-dialog.tsx` (Radix). Cancelar conserva save. | Ninguno. |
| REP-16 Recharts width/height -1 | P2 (Medium) | ✅ | PR #4: `data-modal.tsx` `w-full aspect-auto h-[280px]`; `ui/chart.tsx` `initialDimension` en `ResponsiveContainer`. Warning silenciado en consola al abrir modal. | Ninguno. |
| REP-18 A11y DataModal dialog | P2 (Medium) | ✅ | PR #4: `role=dialog`, Escape, backdrop, **focus-trap** Tab/Shift+Tab, foco inicial y restauración al cerrar (`data-modal.tsx`). | Extender focus-trap a `event-modal` / `initiative-selector` (futuro, no bloqueante). |
| REP-19 Tests de persistencia | P2 (Medium) | ✅ | `tests/use-game-persistence.test.ts` (jsdom): round-trip, clear, TTL, versión, JSON corrupto. | Menor: saves v2 parciales fuera de scope. |
| REP-20 Hardening glosario/dock | Low | ✅ | PR #4: `metricThemes` tipado en `kpi-strip.tsx`; `isGameOver` en dock; labels en engine vía `KPI_SHORT`. | Ninguno. |

## Resumen

### Shipped (✅) — todo en `master`
- **P0:** REP-10, REP-12, REP-13
- **P1:** REP-11, REP-14, REP-15, REP-17
- **P2 / bonus:** REP-16, REP-18, REP-19, REP-20

### Regresiones (❌)
- **Ninguna.** Copy 100% español, `office-scene` intacto, tests y build verdes.

### Próximo lote sugerido (no implementado)
1. Focus-trap en `event-modal` e `initiative-selector` (extensión de REP-18).
2. Playwright smoke E2E versionado en repo (opcional).
3. Alinear labels narrativos de oficina con glosario KPI (decisión de diseño pendiente).

## Historial de auditorías

| Fecha | Run | Resultado |
|---|---|---|
| 2026-06-26 | Auditoría post-P0/P1 (PR #3 abierto) | REP-16/18 ⚠️; sin ❌ |
| 2026-06-27 | Merge PR #3 + #4; doc actualizado | REP-10..20 ✅ en `master` |
