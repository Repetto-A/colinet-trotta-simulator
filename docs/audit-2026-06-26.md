# Auditoría UX — REP-10..19 (2026-06-26)

Auditoría read-only del trabajo P0 (mergeado a `master`) y P1 + bonus (PR #3, abierto:
`cursor/p1-ux-polish-rep-11-14-15-17-e71f`). Metodología: 5 subagentes de revisión de código en
paralelo + smoke manual en navegador (desktop + mobile 390px) + escaneo de consola. Playwright no
está en el repo; el smoke fue manual.

## Estado del repo

- `master`: P0 mergeado (REP-10/12/13). `pnpm test` **55/55**, `typecheck` OK, `build` OK. Node v22.14.0, pnpm 10.33.3.
- PR #3 (branch P1): `pnpm test` **62/62** (7 nuevos de persistencia), `typecheck` OK, `build` OK.
- Diff PR #3 vs `master`: 12 archivos, **sin tocar `office-scene`**, sin archivos inesperados.

## Tabla de auditoría

| Issue | Prioridad | Estado | Evidencia | Gap / follow-up |
|---|---|---|---|---|
| REP-10 Unificar ciclo (12 meses) | P0 (Urgent) | ✅ | `lib/game-balance.ts:8` `CYCLE_TURNS=12`, `:157` guard `turn > MAX_TURNS`; `strategic-header.tsx:71` `Mes {turn}/{CYCLE_TURNS}`, `:46` progreso /CYCLE_TURNS; `cycle-months.ts` 12 meses; `game-content.ts` "12 meses". Sin `/10` de ciclo ni "10 meses". Mergeado en `master`. | Ninguno. |
| REP-12 CTA "Siguiente mes" | P0 (Urgent) | ✅ | `strategic-header.tsx:83-97` botón real solo si `canCloseCycle\|\|isGameOver`; si no, hint `role=note` "Ejecutá una decisión del deck…". Mobile cubierto por `mobile-decision-dock.tsx` (CTA "Decidir" activa). Mergeado. | Menor: el dock no recibe `isGameOver` (en `isGameOver && !canCloseCycle` muestra "Decidir" en vez de "Ver resultado"); no es falsa affordance (botón activo). |
| REP-13 "Jugar otro escenario" | P0 (Urgent) | ✅ | `app/page.tsx:95-103` `handlePlayAgain` → `setCurrentScreen("scenario")` + reset de `selectedScenario/cycleResult/recap/showSeasonReport/loopOverrides`; `results-screen.tsx:130` llama `onPlayAgain`. Mergeado + smoke previo (video). | Ninguno. |
| REP-11 Glosario unificado KPIs | P1 (High) | ✅ | `lib/kpi-glossary.ts` única fuente (`KPI_GLOSSARY`/`KPI_SHORT`); `kpi-strip.tsx`, `welcome-game-preview.tsx`, `game-content.ts` la consumen; `Caja/Equipo/Ritmo` ya no aparecen como labels de KPI. Smoke: preview muestra Presupuesto/Clientes/Control/Capacidad/Velocidad. | Menor (drift-risk, no user-visible): `game-content.ts:17` `tensionPillars[0]="Clientes"` y `game-engine.ts:83-89,508-514` replican labels como literales en vez de importar `KPI_SHORT`. Oficina (`Cliente/Producto GAUS/Cumplimiento`) sin cambios por decisión de diseño (narrativa). |
| REP-15 Pill abre detalle | P1 (High) | ✅ | `kpi-strip.tsx:156-161` cada pill es `<button onClick aria-label>`; contenedor `div role=group` (`:277`); badge "Detalle" botón (`:283`); `onClick` threadeado vía `renderPills`. Smoke desktop+mobile: clic en pill abre "KPIs en detalle". | Ninguno. |
| REP-17 "Detalle" visible en mobile | P1 (High) | ✅ | `kpi-strip.tsx:287` badge con `group flex …` (sin `hidden sm:flex`); es `shrink-0` fuera del scroll → siempre visible. Smoke mobile 390px: "DETALLE >" visible. | Ninguno. |
| REP-14 Confirmar "Nuevo mandato" | P1 (High) | ✅ | `welcome-screen.tsx:103-130` con `hasSave` envuelve "Nuevo mandato" en `AlertDialog`; `onStart` solo desde `AlertDialogAction`; sin save, botón directo. `components/ui/alert-dialog.tsx` (Radix). Smoke: Cancelar conserva save; confirmar va a selección. | Ninguno. |
| REP-16 Recharts width/height -1 | P2 (Medium) | ⚠️ | `data-modal.tsx`: `ResponsiveContainer` redundante eliminado (BarChart hijo directo de `ChartContainer`). PERO el warning `width(-1) and height(-1)` persiste, originado en `components/ui/chart.tsx:65-69` (el `ResponsiveContainer` propio del primitivo + base `flex aspect-video`). Confirmado en consola al abrir el modal. No bloqueante (charts renderizan). | Fix propuesto (deferido, no test-cubierto): añadir `w-full` al `ChartContainer` en `data-modal.tsx:35`, y/o ajustar el primitivo compartido `ui/chart.tsx` (display/altura mínima). |
| REP-18 A11y DataModal dialog | P2 (Medium) | ⚠️ | `data-modal.tsx:116-128` `role=dialog` + `aria-modal` + `aria-labelledby`/`id`, cierre con Escape (`:88-94`) y backdrop (`:120-122`). Smoke: Escape cierra. | Falta **focus trap** y foco inicial (Tab puede salir del modal). Follow-up en REP-18. |
| REP-19 Tests de persistencia | P2 (Medium) | ✅ | `tests/use-game-persistence.test.ts` (`// @vitest-environment jsdom`): round-trip, clear, TTL 24h (+removal), versión inválida, JSON corrupto, `formatSaveSummary`. `jsdom` en devDeps. 62/62. | Menor: no se testea `normalizeGameState` sobre saves v2 parciales (fuera de los 6 escenarios pedidos). |

## Resumen

### Shipped y cumple AC (✅)
- **P0 (en `master`):** REP-10, REP-12, REP-13 — completos.
- **P1 (en PR #3):** REP-11, REP-14, REP-15, REP-17 — completos y verificados (código + smoke desktop/mobile).
- **Bonus (en PR #3):** REP-19 — completo.

### Parciales (⚠️) con propuesta mínima
- **REP-16:** causa señalada por la auditoría (doble `ResponsiveContainer`) resuelta; queda un warning transitorio de montaje del primitivo compartido `ui/chart.tsx`. Propuesta: `w-full` en el uso de `ChartContainer` (`data-modal.tsx:35`) y/o ajuste del primitivo. No bloqueante; deferido (no cubierto por tests).
- **REP-18:** atributos ARIA + Escape + backdrop OK; falta focus-trap/foco inicial. Propuesta: trap de foco (Radix Dialog o manejo manual de Tab) — deferido.

### Regresiones (❌)
- **Ninguna.** Sin scope creep (office-scene intacto), sin código muerto nuevo relevante, copy 100% en español (registro `vos`), sin errores rojos en consola durante el flujo. (El warning de Recharts es pre-existente y no bloqueante.)

### Próximo lote recomendado (NO implementado en este run)
1. **REP-16** (P2): cerrar el warning de Recharts con `w-full` + revisión del primitivo `ui/chart.tsx`.
2. **REP-18** (P2): focus-trap/foco inicial en DataModal; extender a `event-modal`/`initiative-selector`.
3. **Endurecer glosario (nuevo, P2 menor):** rutear `game-engine.ts` labels y `tensionPillars[0]` por `KPI_SHORT`; tipar `metricThemes` como `Partial<Record<…>>`; pasar `isGameOver` al `MobileDecisionDock`.

## Política de fixes aplicada en este run
Sin ❌ bloqueantes → **no se modificó código** (solo este documento de auditoría). Los ⚠️ (REP-16, REP-18) quedan abiertos con propuesta mínima; los ítems menores nuevos se trackean en un issue de follow-up.
